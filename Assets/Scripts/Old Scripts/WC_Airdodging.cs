using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class WC_Airdodging : WalkingController {

    public static void checkAirDodge(InputData data)
    {
        Vector3 direction;
        if (!Grounded() && (data.buttons[0] == true || data.buttons[3] == true))
        {
            direction.y = data.axes[0];
            direction.x = data.axes[1];
            direction.z = 0.0f;
            airDodgeVel = (direction * 3f);
        }
    }

    private static void setPlayerState(PlayerState updateState)
    {
        curState = updateState;
    }
}
*/